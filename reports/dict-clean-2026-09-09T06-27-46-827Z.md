# Báo Cáo Kiểm Tra & Dọn Rác Từ Điển (13:27:46 9/9/2026)

- Nguồn: `local`
- Chế độ: `DRY-RUN`

## Từ điển: `custom`

- **Tổng số từ**: 234
- **Tier A**: 5
- **Tier B**: 12
- **Cụm trùng lặp mờ**: 2

### ❌ Danh sách Tier A (5 từ):
- `ChatGPT`: Tỉ lệ nguyên âm quá thấp (< 20%) trên toàn từ
- `HTTPS`: Chứa chuỗi liên quan file/URL (pdf, http, www, .com, .net, .org), Chứa 5 phụ âm liên tiếp trở lên
- `III`: Chuỗi âm thanh lặp lại (tiếng cười/thán từ)
- `MSNBC`: Chứa 5 phụ âm liên tiếp trở lên
- `SMERSH`: Tỉ lệ nguyên âm quá thấp (< 20%) trên toàn từ

### ⚠️ Danh sách Tier B (12 từ):
- `GitHub`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `GitLab`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `JavaScript`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `KakaoTalk`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `LinkedIn`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `PayPal`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `SpaghettiOs`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `TikTok`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `TypeScript`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `WeChat`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `WiFi`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)
- `YouTube`: Cụm TitleCase dính liền (có thể là họ ghép hoặc lỗi merge)

